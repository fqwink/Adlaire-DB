# Adlaire DB 仕様書

**バージョン：** 0.1（白紙草案）  
**ステータス：** 設計中  
**最終更新：** 2026-09-10  

---

## 1. 概要

### 1.1 プロジェクト概要

Adlaire DB は **libSQL フォーク**をストレージエンジン・SQL エンジンの全体基盤として採用した、Rust 製シングルバイナリ DB サーバーである。  
libSQL フォークが提供する SQL パーサ・クエリエグゼキューター・WAL・ページストレージをそのまま活用し、その上に独自のサーバー層・運用機能・[TBD] を構築する。

外部 DB ライブラリへの依存は **libSQL フォーク一本に限定** する。libSQL フォーク以外の外部 DB ライブラリは使用しない。

### 1.2 固定制約

| 項目 | 値 |
|------|-----|
| 実装言語 | Rust |
| 依存ライブラリ方針 | Rust 標準ライブラリ＋ libSQL フォーク。暗号・TLS・HTTP は別途検討 |
| ストレージ基盤 | libSQL フォーク（全体）|
| デプロイ形態 | シングルバイナリ起動 |
| 対象 OS | Linux（主）|

### 1.3 設計目標

[TBD] — libSQL フォーク上で Adlaire DB が提供する差別化機能・運用上の要件を定義する。

---

## 2. アーキテクチャ

### 2.1 全体構成

```
クライアント
     │
     ▼
┌─────────────────────┐
│  Adlaire サーバー層  │  ← Rust 実装（詳細 TBD）
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   libSQL フォーク    │  SQL パーサ / クエリ実行 / WAL / ページストレージ
└─────────────────────┘
```

### 2.2 libSQL フォークの利用範囲

libSQL フォーク（libSQL を Adlaire DB 専用に改変したもの）が担う責務：

- SQL パーサ・クエリプランナー・クエリエグゼキューター
- WAL（Write-Ahead Log）管理
- ページストレージ（B-Tree ページ形式）
- SQLite 互換レイヤー（維持するか否かは [TBD]）

Adlaire DB が libSQL フォークに加える変更・拡張：

- [TBD]

### 2.3 サーバー層

クライアントとの通信プロトコル、トランザクション管理、認証、運用機能など：

- [TBD]

---

## 3. データモデル

[TBD]

---

## 4. API 仕様

[TBD]

---

## 5. トランザクション管理

[TBD]

---

## 6. 監査・データ保全

[TBD]

---

## 7. 実装フェーズ

[TBD] — フェーズ構成・各フェーズの完了条件を定義する。

---

## 8. 配布・デプロイ

[TBD]

---

## 付録：用語定義

| 用語 | 定義 |
|------|------|
| libSQL フォーク | Adlaire DB 専用に改変した libSQL のこと。本プロジェクトのストレージ・SQL 基盤全体を指す |
| Adlaire サーバー層 | libSQL フォークの上に構築する Rust 製サーバーコンポーネント群 |
